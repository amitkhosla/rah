# ─── Semantic Cache APIs ──────────────────────────────────────────────────────
#
# API definitions for the semantic cache sample.
# Flows referenced here are defined in instructions/10-semantic-cache.yaml.
# ─────────────────────────────────────────────────────────────────────────────

apis:

  - name: sample-ai-semantic-cache
    path: /samples/ai/semantic
    method: POST
    flow_name: api_semantic_cache
    action: upsert
    skip_rate_limit: true
