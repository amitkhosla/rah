datastore:
  stores:
    local:
      name: local
      kind: disk
      enabled: true
      connection:
        path: /tmp/rah-data
  bindings:
    api_definitions: local
    flows: local
    tenant_data: local
    cache: local
